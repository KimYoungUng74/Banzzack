<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductMapper">
	<!-- id는 마음대로 작성 prarameter는 실제DTO클래스 -->

	<!-- 제품 등록 -->
	<insert id="Register" parameterType="ProductDTO">
		INSERT INTO PRODUCT_TABLE(PRODUCT_NUM, PRODUCT_TITLE, PRODUCT_CONTENTS, PRODUCT_IMG1, PRODUCT_IMG2, PRODUCT_IMG3, PRODUCT_RPICE, PRODUCT_COUNT, PRODUCT_CATEGORY1, PRODUCT_CATEGORY2)
		VALUES(PRODUCTAUTOINC.nextval, #{PRODUCT_TITLE}, #{PRODUCT_CONTENTS}, #{PRODUCT_IMG1},
		#{PRODUCT_IMG2}, #{PRODUCT_IMG3}, #{PRODUCT_RPICE}, #{PRODUCT_COUNT}, #{PRODUCT_CATEGORY1}, #{PRODUCT_CATEGORY2})
	</insert>
	<!-- 가상이미지 등록 -->
	<insert id="VirtualRegister" parameterType="VirtualDTO">
		INSERT INTO VIRTUAL_TABLE(PRODUCT_NUM, VIRTUAL_IMG, VIRTUAL_IMG2)
		VALUES(VIRTUALCTAUTOINC.nextval, #{VIRTUAL_IMG}, #{VIRTUAL_IMG2})
	</insert>
	<!-- 제품 리스트 전체 조회 -->
	<select id="productListAll" parameterType="ProductDTO"
		resultType="ProductDTO">
		SELECT
		PRODUCT_NUM,
		PRODUCT_TITLE,
		PRODUCT_IMG1,
		PRODUCT_RPICE
		FROM PRODUCT_TABLE
		WHERE PRODUCT_CATEGORY1 = #{PRODUCT_CATEGORY1}
	</select>
	<!-- 제품 리스트 전체 조회 -->
	<select id="productListSelect" parameterType="ProductDTO"
		resultType="ProductDTO">
		SELECT
		PRODUCT_NUM,
		PRODUCT_TITLE,
		PRODUCT_IMG1,
		PRODUCT_RPICE
		FROM PRODUCT_TABLE
		WHERE PRODUCT_CATEGORY1 = #{PRODUCT_CATEGORY1} 
		AND PRODUCT_CATEGORY2 = #{PRODUCT_CATEGORY2}
	</select>
	<!-- 제품 정보 조회 -->
	<select id="ProductSearch" parameterType="ProductDTO"
		resultType="ProductDTO">
		SELECT
		PRODUCT_NUM,
		PRODUCT_TITLE,
		PRODUCT_CONTENTS,
		PRODUCT_IMG1,
		PRODUCT_IMG2,
		PRODUCT_IMG3,
		PRODUCT_RPICE,
		PRODUCT_COUNT,
		PRODUCT_CATEGORY1,
		PRODUCT_CATEGORY2
		FROM PRODUCT_TABLE
		WHERE PRODUCT_NUM = #{PRODUCT_NUM}
	</select>
	
	<select id="bestSellerproduct" parameterType="ProductDTO"
		resultType="ProductDTO">
		SELECT  SUM(o.orders_amount) 구매수량,
        p.PRODUCT_NUM,
		PRODUCT_TITLE,
		PRODUCT_IMG1,
		PRODUCT_RPICE
		FROM ORDERS_TABLE o, product_table p
		where o.product_num = p.product_num AND PRODUCT_CATEGORY1 = #{PRODUCT_CATEGORY1} 
		group by p.PRODUCT_NUM,PRODUCT_TITLE,PRODUCT_IMG1,PRODUCT_RPICE
		ORDER BY SUM(o.orders_amount) DESC
	</select>
		
	<!-- 가상 착용 정보 조회 -->
	<select id="VirtualSearch" parameterType="VirtualDTO"
		resultType="VirtualDTO">
		SELECT
		PRODUCT_NUM,
		VIRTUAL_IMG,
		VIRTUAL_IMG2
		FROM VIRTUAL_TABLE
		WHERE PRODUCT_NUM = #{PRODUCT_NUM}
	</select>

</mapper>